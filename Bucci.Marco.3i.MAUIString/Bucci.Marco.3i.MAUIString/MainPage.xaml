<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Bucci.Marco._3i.MAUIString.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Spacing="25"
            Padding="30,0"
            VerticalOptions="Center">

            <Editor
                x:Name="StringIn"
                Text="Input"
                FontSize="18"
                HorizontalOptions="Center" 
                HeightRequest="200"
                WidthRequest="500"
                HorizontalTextAlignment="Center"
                VerticalTextAlignment="Center"
                />

            <Label
                Text="Output"
                x:Name="StringOut"
                FontSize="18"
                HorizontalOptions="Center"
                />

            <Button
                x:Name="Button"
                Text="Click me"
                Clicked="StringBtn"
                HorizontalOptions="Center" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
